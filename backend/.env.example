# Server Configuration
PORT=3001
NODE_ENV=production

# Firebase Admin SDK Configuration (REQUIRED for Render)
# Get these from your Firebase Console -> Project Settings -> Service Accounts
FIREBASE_PROJECT_ID=your_firebase_project_id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour private key here\n-----END PRIVATE KEY-----\n"

# Alternative: Path to service account JSON file (for local development)
# FIREBASE_SERVICE_ACCOUNT_PATH=./serviceAccountKey.json

# CORS Configuration (REQUIRED)
# Add your frontend URLs separated by commas
ALLOWED_ORIGINS=https://your-frontend-domain.com,https://your-app.vercel.app

# Rate Limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# Gemini API Key (REQUIRED for AI comment suggestions)
GEMINI_API_KEY=your_gemini_api_key_here

# Reddit OAuth Configuration (OPTIONAL - for user subreddit syncing)
# Register your app at https://www.reddit.com/prefs/apps
REDDIT_CLIENT_ID=your_reddit_client_id
REDDIT_CLIENT_SECRET=your_reddit_client_secret
# Development redirect URI
REDDIT_REDIRECT_URI=http://localhost:5173/auth/reddit/callback
# Production redirect URI (uncomment and update for production)
# REDDIT_REDIRECT_URI=https://your-production-domain.com/auth/reddit/callback

# Encryption Key (REQUIRED for storing sensitive data)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# IMPORTANT: Keep this secret and never commit to version control
ENCRYPTION_KEY=your_base64_encoded_32_byte_key_here

# Scheduled Jobs
RUN_FETCH_ON_STARTUP=false
