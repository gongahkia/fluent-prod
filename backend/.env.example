# Server Configuration
PORT=3001
NODE_ENV=development

# AI Services (Google Gemini)
# Get your free API key from: https://ai.google.dev/
GEMINI_API_KEY=your_gemini_api_key_here

# Firebase Admin SDK Configuration
# Option 1: Use service account file (recommended for local development)
# Download service account JSON from Firebase Console > Project Settings > Service Accounts
# FIREBASE_SERVICE_ACCOUNT_PATH=./path/to/serviceAccountKey.json

# Option 2: Use environment variables (recommended for production/Render)
# Get these values from your Firebase service account JSON file
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour private key here\n-----END PRIVATE KEY-----\n"

# Note: FIREBASE_STORAGE_BUCKET is optional - we use Firestore for posts caching (100% free tier)

# Scheduled Jobs Configuration
# Set to 'true' to run posts fetch job immediately on server startup (useful for testing)
RUN_FETCH_ON_STARTUP=false

# NOTE: Twitter and Instagram API credentials are now configured via the frontend
# Users can enter their API keys in Profile > Developer Mode tab
# These credentials are stored in the browser's sessionStorage and sent with API requests

# Rate Limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# CORS Configuration
# Local development:
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
# For production deployment, add your deployed frontend URLs:
# ALLOWED_ORIGINS=http://localhost:5173,https://your-app.vercel.app,https://your-custom-domain.com
